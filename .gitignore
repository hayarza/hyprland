# Timestamped backups written by omarchy refresh / manual edits
*.bak
*.bak.*

# Guard: keep any backup dir out of the repo. Archives live outside the
# working tree, in ~/.local/state/config-backups/hypr/.
backups/
